branches:
    only:
        - master

language: node_js
node_js:
    - node

notifications:
    email: false

sudo: required
dist: trusty

before_script:
    - export DISPLAY=:99.0; export XVFBARGS="+extension RANDR"; sh -e /etc/init.d/xvfb start
