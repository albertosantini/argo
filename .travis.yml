branches:
    only:
        - master

language: node_js
node_js:
    - node

notifications:
    email: false

sudo: required
dist: trusty

before_script:
    - export DISPLAY=:99.0; sh -e /etc/init.d/xvfb start
